import React, { useState } from 'react';

function App() {
  if (true) {
    const [a, setA] = useState('invalid');
  }

  return (
    <div>
      <h1>Webpack starter</h1>
    </div>
  );
}

export default App;
